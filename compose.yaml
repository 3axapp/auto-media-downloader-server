services:
  app:
    build: tests
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-8080}:8080"
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      DB_PATH: /data/amd.db
      HTTP_PORT: "8080"
